import 'package:flutter/material.dart';

class AppPallete {
  static const Color backgroundColor = Color.fromRGBO(24, 24, 32, 1);
  static const Color gradient1 = Color.fromRGBO(187, 63, 221, 1);
  static const Color gradient2 = Color.fromRGBO(251, 109, 169, 1);
  static const Color gradient3 = Color.fromRGBO(255, 159, 124, 1);
  static const Color borderColor = Color.fromRGBO(52, 51, 67, 1);
  static const Color whiteColor = Colors.white;
  static const Color greyColor = Colors.grey;
  static const Color errorColor = Colors.redAccent;
  static const Color transparentColor = Colors.transparent;
}

class AppPalleteLight {
  static const Color backgroundColor =
      Color.fromRGBO(245, 245, 250, 1); // Light background
  static const Color primaryColor =
      Color.fromRGBO(136, 84, 208, 1); // Soft purple for primary elements
  static const Color secondaryColor =
      Color.fromRGBO(255, 143, 148, 1); // Pinkish hue for accents
  static const Color borderColor =
      Color.fromRGBO(220, 220, 230, 1); // Light border color
  static const Color blackColor = Colors.black; // Text and icons
  static const Color greyColor =
      Color.fromRGBO(150, 150, 150, 1); // Muted grey for secondary text
  static const Color errorColor = Colors.redAccent;
  static const Color transparentColor = Colors.transparent;
}
